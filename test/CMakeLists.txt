set(TEST_NAME test)

set(TEST_DIR "${CMAKE_CURRENT_SOURCE_DIR}")
set(TEST_SOURCE_DIR "${TEST_DIR}")

file(GLOB_RECURSE TEST_SOURCE_FILES "${TEST_SOURCE_DIR}/*.htm" "${TEST_SOURCE_DIR}/*.css" "${TEST_SOURCE_DIR}/*.js")

add_custom_target(${TEST_NAME} ALL SOURCES ${TEST_SOURCE_FILES})

add_custom_command(TARGET ${TEST_NAME} POST_BUILD COMMAND ${CMAKE_COMMAND} ARGS -E copy ${TEST_SOURCE_FILES} "${CMAKE_CURRENT_BINARY_DIR}")
